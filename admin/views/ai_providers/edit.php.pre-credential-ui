<?php use function App\Core\e; ?>
<h1 class="h3 mb-4">Edit AI Provider</h1>

<?php if ($error): ?><div class="alert alert-danger"><?= e($error) ?></div><?php endif; ?>

<div class="alert alert-warning">No credential field exists here by design — see AI Providers list page.</div>

<div class="card" style="max-width: 640px;">
    <div class="card-body">
        <form method="POST" action="/admin/ai-providers/<?= (int) $provider['id'] ?>">
            <?= \App\Auth\Csrf::field() ?>
            <div class="mb-3">
                <label class="form-label">Name (internal key)</label>
                <input type="text" class="form-control" disabled value="<?= e($provider['name']) ?>">
            </div>
            <div class="mb-3">
                <label class="form-label">Display name</label>
                <input type="text" name="display_name" class="form-control" maxlength="150" value="<?= e($provider['display_name'] ?? '') ?>">
            </div>
            <div class="mb-3">
                <label class="form-label">Model registry (one per line)</label>
                <textarea name="model_registry" class="form-control" rows="3"><?= e(implode("\n", json_decode($provider['model_registry'] ?? '[]', true) ?: [])) ?></textarea>
            </div>
            <div class="form-check mb-3">
                <input type="checkbox" name="is_enabled" id="is_enabled" class="form-check-input" <?= (int) $provider['is_enabled'] === 1 ? 'checked' : '' ?>>
                <label class="form-check-label" for="is_enabled">Enabled</label>
            </div>
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="/admin/ai-providers" class="btn btn-link">Cancel</a>
        </form>
    </div>
</div>
